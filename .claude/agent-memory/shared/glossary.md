# 项目术语表

> 所有子 agent 共享。累积跨变更的领域知识。

| 术语 | 含义 | 首次记录于 |
|------|------|-----------|
| ProblemDetails (RFC 9457) | 标准化的 HTTP API 错误响应格式，用于全局异常处理的统一返回结构 | 四合一架构重构 |
| 双重写路径 (Dual Write Path) | 消息持久化存在两个独立的写入路径导致数据不一致的架构问题 | 四合一架构重构 |
| 配置漂移 (Configuration Drift) | 代码中的配置值（如 MaxMessages=2）与设计文档约定值（如 20）不一致的现象 | 四合一架构重构 |
| IExceptionHandler | .NET 10 内置的全局异常处理接口，无需额外 NuGet 包 | 四合一架构重构 |
| ModelInfo | `public record ModelInfo(string Id, string Name, bool IsDefault)` 表示一个 AI 模型的信息，Id 为配置键，Name 为显示名称，IsDefault 标记是否为默认模型 | T3 |
| ModelRouter | 单例服务，管理多个 AI 模型的配置读取、Agent 实例创建、模型信息查询 | T3 |
| 模型卡片选择 | 登录页面的三列网格布局，每张卡片对应一个 AI 模型，支持点击切换选中态，isDefault 模型自动高亮 | T8 |
| 幂等建表兜底 | EnsureCreatedAsync 对既有库不补建新表，Program.cs 在播种前用 CREATE TABLE IF NOT EXISTS 补建缺失表，双路径（全新库/既有库）均幂等 | product-catalog-persistence |
| UserPreferenceUpdate | 偏好写入队列的轻量消息 record(Guid UserId, IReadOnlyList<string> Preferences) | product-catalog-persistence |
| 读-改-写串行化 | 偏好累加收敛到单一 worker，按序读旧值→累加→写库，天然避免并发丢累加 | product-catalog-persistence |
| 双轨合一 | 商品读取收敛为单一数据源（ProductCatalog.All 走 ProductRepository 缓存），消除同请求快照与缓存双轨不一致 | product-catalog-persistence |
| worker 侧累加 | 端点只入队轻量消息，权重累加在 PreferenceWriteHostedService 内执行 | product-catalog-persistence |
| ProductKeywordMap.Entries | 商品关键词白名单映射（23 组 key→标签扩展数组），Core 静态常量，从 ProductCatalog.KeywordMap 迁出，供 Agent 指令与推荐合并复用 | product-catalog-persistence |
| UserPreferences | 用户偏好持久化实体：主键 UserId，KeywordsJson 存 `{关键词:权重}` JSON，UpdatedAt 为最后更新时间；由后台 worker 异步 upsert 落库，每次 /chat 从 DB 回填 | product-catalog-persistence |
| RecommendationMerger | 推荐合并纯函数（public static）：GetTopPreferenceKeywords 反序列化偏好权重按降序取 Top-N；MergeKeywords 当前关键词优先、不足 3 个才用偏好补齐到 ≤5、OrdinalIgnoreCase 去重；供 /chat 与 /recommendations 复用 | product-catalog-persistence |
| ProductSeedData.Products | 18 个商品的种子常量（Core 静态类，Id 显式 1..18），从 ProductCatalog.All 迁出的唯一数据源，供 Program.cs 幂等播种与测试兜底复用；ProductCatalog 改造完成前与 All 硬编码列表并存属预期中间态 | product-catalog-persistence |
| PreferenceQueue | 偏好写入队列的 Channel 实现（有界容量 64 / DropOldest / SingleReader），`TryEnqueue` 非阻塞委托 `Writer.TryWrite`，`static Create()` 工厂；`Reader` 供 PreferenceWriteHostedService 单 worker 串行消费。**DropOldest 语义：满队时挤出最旧再写入并返回 true，仅 channel 标记完成后 TryEnqueue 返回 false**；入队前若 `Reader.Count >= 64`（本次必挤旧）记录 `Log.Warning` 近似观测丢弃（P2-3，Infrastructure 引 Serilog 静态 Log） | product-catalog-persistence |
| ChatOptions.Instructions 偏好注入路径 | MAF 中 AIContextProvider 返回的 `AIContext.Instructions` 由 HarnessAgent 合入 **`ChatOptions.Instructions`**（LLM system prompt），而非 `ChatMessage` 列表；测试断言"LLM 输入含偏好文本"需读捕获的 `ChatOptions.Instructions` | T16 (product-catalog-persistence) |
| products_all 缓存键 | `ProductRepository.GetAll()` 的 IMemoryCache 缓存键，5 分钟绝对过期；首次查 `db.Products.ToList()` 后进缓存，`/products`、Agent 搜索工具、`ProductCatalog.All`（T8 起）统一走该缓存，单一数据源 | product-catalog-persistence |
| EF SQLite PascalCase 表名 | `EnsureCreated` 对 `AppDbContext` 的 `Product`/`UserPreferences` 实体生成 **PascalCase** 表名与约束名：`"Products"`/`"UserPreferences"`/`PK_Products`/`PK_UserPreferences`（SQLite 不分大小写但 EF 元数据按此存储）；Program.cs 幂等建表兜底 DDL 必须照抄该命名，否则 EF 查询因表名不匹配失败 | product-catalog-persistence (T7) |
| PreferenceWriteHostedService | 偏好异步写入后台服务（`BackgroundService` 子类），单 worker 串行消费 `PreferenceQueue.Reader`：读旧值 → 逐词 +1 → Top-20 截断 → upsert 写库；构造函数注入具体 `PreferenceQueue`（`IPreferenceQueue` 无读端）与 `IDbContextFactory<AppDbContext>`，优雅关闭捕获 OCE 正常退出；T15 DI 注册需同时注册具体 `PreferenceQueue` 并映射到接口 | product-catalog-persistence (T14) |
| worker 单条异常容错 | PreferenceWriteHostedService 对每条消息的「读-改-写」包内层 try/catch（`when (ex is not OperationCanceledException)`）：单条处理失败（如 `KeywordsJson` 改坏 → JsonException、SQLite 写锁 → SqliteException）`LogWarning` 后 `continue`，不因单条异常导致 `BackgroundService` 永久死亡；OCE 仍传播由外层捕获正常关闭 | product-catalog-persistence (R1) |
| Top-20 按权重截断 | 偏好权重表超 20 词时按权重降序保留前 20，并列权重按 Key 序数序（`OrderByDescending(Value).ThenBy(Key, StringComparer.Ordinal).Take(20)`）二次排序保证可复现；不再依赖 `Dictionary` 枚举顺序（旧 `Take(20)` 保留的是最早见过的 20 个词） | product-catalog-persistence (R1) |
| 对话历史商品 ID 隐藏（R4/R5） | 服务端 `SanitizeReply` 清洗 `ChatReply.Response` 文本：`#数字` 仅删商品 ID 1-18（`#(?<id>\d+)` 提取 + `int.TryParse` 校验，处理中文粘连 `#3无线`），`商品ID[:：]数字` 保持宽匹配；对话历史不显示商品 ID。`RecommendedProducts`/`OtherProducts` 的 `ProductDto.Id` 不变（前端加购来源），购物车不受影响 | product-catalog-persistence (R4/R5) |
| 纯偏好驱动推荐（R6） | `/api/recommendations` 重构为纯用户偏好驱动：删除 LLM Agent 调用（`defaultAgent.RunChatAsync`）、对话历史读取（`lastUserMessage`）与缓存读写（`reco_`/`agent_result_`），只按 `UserPreferences` 权重 Top-5 经 `RecommendationMerger.MergeKeywords([], prefKeywords)` 出推荐；无偏好时 `All.Take(6)` 兜底。推荐只随偏好不随消息；推荐/兜底列表经轻量 Fisher-Yates `ShuffleProducts`（`Random.Shared`，只变顺序不变内容）防固化 | product-catalog-persistence (R6) |
| 随对话内容推荐（R7，取代 R6 纯偏好驱动） | `/api/recommendations` 改为「最新用户消息关键词优先 + 偏好补齐」驱动：`chatRepo.GetLastUserMessageAsync(sid)` 读最新用户消息 → `catalog.KeywordMap` 直接匹配 `validKeywords`（与 /chat L128-136 同逻辑，不调 LLM，毫秒级）→ `MergeKeywords(validKeywords, prefKeywords)`（`prefKeywords` 保留 P2-4 白名单过滤）→ `SplitProducts`。**删除 R6 shuffle**（固定顺序、多次调用确定性）。提示语：有推荐 →「根据您的对话，为您推荐：」；无关键词无偏好 →「为您精选商品」（非「暂无特定推荐」）；仅 `catalog.All.Count == 0` 才「暂无特定推荐」 | product-catalog-persistence (R7) |
| 推荐快照缓存联动（R8，取代 R7 读库消息匹配） | `/api/chat` 算完 `ChatReply` 后写**用户维度内存缓存** `recommend_{username}`（TTL 10min，存 `RecommendationSnapshot`：Recommended/Other/MatchedCategories/HasRecommendation/Message）；`/api/recommendations` **优先读缓存**——命中直接按快照构造 `RecommendationResponse`（BestMatch=快照 Recommended 首项、Other/Message/MatchedCategories 全取快照，与聊天 100% 一致，**不再读 DB 最新消息做字面匹配**）；miss（新用户/缓存过期）走偏好兜底（`FilterValidPreferenceKeywords` + `MergeKeywords([], prefKeywords)` + `SplitProducts`）→ 精选兜底（`All.Take(6)` +「为您精选商品」）。删除 R6 残留的 `agent_result_{username}_{hash}` 死代码与 `GetMessageHash`；/recommendations 移除 `sessions`/`chatRepo` 依赖。提示语以聊天快照为准（推荐「根据您的兴趣，为您推荐：」/ 兜底「暂无特定推荐 — 浏览精选商品」） | product-catalog-persistence (R8) |
| RecommendationSnapshot | 用户维度推荐快照 record（`List<ProductDto>? Recommended`/`Other`、`string[]? MatchedCategories`、`bool HasRecommendation`、`string Message`），由 `/api/chat` 每次写 `recommend_{username}` 缓存、`/api/recommendations` 读缓存复用，保证推荐栏=聊天推荐镜像 | product-catalog-persistence (R8) |
| RecommendationResponse.Recommended（R8.1） | `RecommendationResponse` 新增 `List<ProductDto> Recommended` 字段（完整推荐列表，含 BestMatch），构造签名 `(ProductDto? BestMatch, List<ProductDto> Recommended, List<ProductDto> Other, string Message, string[]? MatchedCategories)`；不变量 `BestMatch == Recommended[0]` 且 `Recommended ∩ Other == ∅`（SplitProducts 天然满足）。缓存命中路径 Recommended=快照完整列表（含枕套等非首项）、偏好兜底有推荐 Recommended=SplitProducts 推荐列表、无推荐 Recommended=[]。前端 `getRecommendations()`→`renderRecommendations(data)` 以 recommended 为主两段式渲染（🎯为您推荐[首个高亮] + 📋其他商品；recommended 空 → 📋全部商品兜底），bestMatch 字段保留兼容 | product-catalog-persistence (R8.1) |
| 商品Id:N 固定格式（R9） | Agent 指令（ShoppingAssistantAgent.BuildInstructions【回复规范】第 3 条）规定的商品 ID 唯一合法展示格式（如「商品Id:4」），其余形式（商品ID为4、#4、编号4、ID：4 等）均属违例；服务端 SanitizeReply 用 `FixedIdPattern`（`商品Id[:：]\d+` + IgnoreCase）精确删该格式，`ProductIdLabelPattern` 字符类扩入 `[\s:：为是]` 兜底删「商品ID为4」「商品ID是4」等变体（字段泄漏案例根因：R4/R5 只覆盖冒号/空格连接，未覆盖「为/是」连接词） | product-catalog-persistence (R9) |
| ChatClientMetadata（R10） | `IChatClient.Metadata` 属性类型。Microsoft.Extensions.AI 10.8.3 构造函数实测为 `(string providerName, Uri? providerUri = null, string? defaultModelId = null)`（第 2 参是 providerUri 非 modelId，模型名走第 3 参 defaultModelId）；属性为 ProviderName/ProviderUri/DefaultModelId（无 ModelId 别名）。DeepSeekChatClient 旧主构造函数未实现 Metadata → 接口默认空对象 → OTel gen_ai 属性（provider/model）缺失；R10 实现 `Metadata = new ChatClientMetadata(providerName: "DeepSeek", defaultModelId: modelName)` 修复 | product-catalog-persistence (R10) |
| login 历史清洗（R10） | `/api/login` 返回的对话历史中 assistant 消息经 `SanitizeReply` 去商品 ID 展示（user 消息原样保留）。背景：SqliteChatHistoryProvider 存 assistant 消息为 `StripAgentReplyJson` 提取的原始 Reply 文本（含商品 ID 标记），登录出口此前直接返回未清洗 → 泄漏 | product-catalog-persistence (R10) |
| gen_ai 遥测属性来源（R10.1） | MEAI 埋点：`gen_ai.request.model` 读 **`request.ChatOptions.ModelId`**（非 IChatClient.Metadata）；`gen_ai.provider.name` 读 **`IChatClient.GetService(typeof(ChatClientMetadata))`** 返回的 `metadata.ProviderName`；request.model 优先级 = `options.ModelId ?? metadata.DefaultModelId`。修复：`DeepSeekDelegatingChatClient.GetResponseAsync` 开头 `options.ModelId ??= _modelName`（分支前，deepseek/base 都生效）+ `DeepSeekChatClient.GetService` 对 `ChatClientMetadata` 返回 Metadata；`DelegatingChatClient.GetService` 为 virtual 且转发 inner（无需在 delegating 包装类覆盖） | product-catalog-persistence (R10.1) |
| 被吞异常进 OTel span（R11） | MEAI 埋点对「抛出异常」只 `SetStatus(Error)` 不产生 exception 事件；「捕获后兜底返回」路径（DeepSeek 非 2xx、ChatEndpoints catch）连 SetStatus 都没有 → Aspire span 只有 error.type 无详情。修复 = 手动 `Activity.Current?.SetStatus(Error, msg)` + `AddEvent(new ActivityEvent("exception", tags: {exception.type, exception.message}))`（`ActivityEvent` 第 2 参是 DateTimeOffset，须用 `tags:` 命名参；`Activity.RecordException` 扩展在当前 DiagnosticSource 版本不可用，用 AddEvent 等价替代）。`ActivityEvent`/`ActivityTagsCollection` 在 `System.Diagnostics` | product-catalog-persistence (R11) |
| run_id | `chat_messages` 轮次分组标识（`ChatMessageRecord.RunId: Guid?`，列 `run_id` TEXT）。一次 `RunChatAsync` 的所有消息共享（Agent 生成写 `Session.StateBag("RunId")`，`SqliteChatHistoryProvider.StoreChatHistoryAsync` 读值打标，FICC 多次 Store 共享）；只分组不排序（`id` 仍是组内顺序锚点）；`run_id=NULL` 历史行每行自成一组走旧行为。孤儿 tool 检查按「同 run_id 组内是否存在 assistant-FCC」配对，NULL 行退化相邻 id 检查 | chat-round-boundary |
| is_final | `chat_messages` 轮次终点标记（`ChatMessageRecord.IsFinal: bool`，列 `is_final` INTEGER 0/1）。`AnyAsync(m => m.RunId==runId && m.IsFinal)` 即该轮完成。写入双路径：Store 内判定为主（末条纯文本 assistant 即迭代结束）+ Run 后兜底补标（`MarkRoundFinalAsync(runId)` 收敛在 Provider，避免新增写入口）。语义以「终点标记」为准——通常为最终回复行，仅调工具场景可为 tool/FCC 行；FICC 耗尽/异常中断的轮无该行 → 未完成轮 | chat-round-boundary |
| 整轮整切压缩 | 压缩由「按条数硬切保留最新 50 条 + ±1 相邻推断保护」改为「按 run_id 整轮整切」：最近 K=12 个完整轮次（纯文本轮 2 行/轮、含工具轮约 4 行/轮，容量约等于旧 50 条）+ 条数硬上限 4K + 未完成轮上限 5 兜底（超限强制压最旧并记 Warning）。整轮同压使 FCC↔tool 配对结构性不分离，删除 ±1 推断逻辑 | chat-round-boundary |
| 未完成轮 | 组内无 `is_final=true` 行的轮（FICC 迭代耗尽 / 异常中断 / 仅调工具未输出文本）。压缩时整组保留；计数口径只统计 `run_id` 非空且 ≥2 行的组，`run_id=NULL` 历史行不计入（防历史库瞬间超上限误压缩） | chat-round-boundary |
| 标准弹性策略（AddStandardResilienceHandler） | `ServiceDefaults` 在 `ConfigureHttpClientDefaults` 里对 `IHttpClientFactory` 管理的 `HttpClient` 自动装配的标准弹性（retry/熔断/超时：`HttpStandardResilienceOptions`，默认 AttemptTimeout 10s、TotalRequestTimeout 30s、retry 3 次、含 429 与 5xx）；**只对命名客户端生效**，`ModelRouter.CreateChatClient` 自建 `HttpClient` 不走此默认，需手动用 `ResilienceHandler` 包装接入 | chat-round-boundary (Phase 6) |
| ResilienceHandler 包装链 | 自建 HttpClient 接入标准弹性的方式：`new ResilienceHandler(pipeline) { InnerHandler = new DebugHandler(httpClientHandler, debugEnabled) }`，pipeline 由 `new ResiliencePipelineBuilder<HttpResponseMessage>().AddTimeout(TimeSpan.FromSeconds(110))`
` + AddRetry(HttpRetryStrategyOptions) + AddCircuitBreaker(HttpCircuitBreakerStrategyOptions).Build()` 构建（T11 实测：`AddStandardResilienceHandler(ResiliencePipelineBuilder)` 重载在锁定 `Microsoft.Extensions.Http.Resilience` 10.7.0 不存在，用等效积木复现标准弹性，语义一致无需升级依赖）；链式 `HttpClient → ResilienceHandler → DebugHandler → HttpClientHandler`（DebugHandler 包在 ResilienceHandler 内层，每次重试尝试可见）。**超时协调**：保留 `HttpClient.Timeout = 120s` 作为外层硬天花板，弹性 TotalRequestTimeout 取略小值（如 110s）先到点，避免双重超时冲突 | chat-round-boundary (Phase 6) |
| 可重试异常分类（IsRetryableAgentFailure） | `/api/chat` 应用层重试判据（ChatEndpoints `internal static bool`）：`HttpRequestException`（网络抖动）/ `TimeoutException` / `TaskCanceledException` 且调用方 ct 未取消（HttpClient 超时）/ `ClientResultException`（`Status` 429 或 5xx，OpenAI/Qwen 路径非 2xx 抛出）→ 值得重试；`KeyNotFoundException`（模型不存在）已有独立 catch 在最前、不重试；`InvalidOperationException` 等协议/解析类错误不重试 | chat-round-boundary (Phase 6) |
| DeepSeek 429 吞兜底 | `DeepSeekChatClient`（直发 HTTP 路径）对非 2xx 不抛异常，而是 `SetStatus(Error)` + 返回兜底 ChatResponse（「抱歉，暂时无法处理您的请求，请重试。」）→ 429 不会到达 `ChatEndpoints` 的 catch；C（传输层弹性）对 429/5xx 在 HttpClient 层自动重试后，吞兜底仅发生在弹性重试耗尽后。A 的 429 分类主要覆盖 OpenAI/Qwen 路径（`ClientResultException`）与逃逸到端点的网络/超时异常；改动 DeepSeekChatClient 抛异常行为超出变更范围 | chat-round-boundary (Phase 6) |
